+++
date = '{{ .Date }}'
lastmod = '{{ .Date }}'
draft = false
title = '{{ replace .File.ContentBaseName "-" " " | title }}'
keywords = ["页面的关键词，用于搜索引擎优化"]
linktitle = "页面在链接中的显示标题"
weight = "页面的权重，用于排序（默认基本1，中等2，高级3）"
tags = ["标签"]
categories = ["类别"]
+++

# 这里开始写博客正文
## 一、什么是 Hugo Stack 主题
Stack 是一款简约、美观、高性能的 Hugo 主题，专注于内容展示，支持响应式布局。

// 正文内容...